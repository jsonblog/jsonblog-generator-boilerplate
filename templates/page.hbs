{{#> layout}}
  {{#*inline "content"}}
    <div class="container">
      <article class="page">
        <header class="page-header">
          <h1>{{page.title}}</h1>
        </header>

        <div class="page-content">
          {{{page.content}}}
        </div>
      </article>
    </div>
  {{/inline}}
{{/layout}}
