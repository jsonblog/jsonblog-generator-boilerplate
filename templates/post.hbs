{{#> layout}}
  {{#*inline "content"}}
    <article>
      <time datetime="{{post.publishedDate}}">{{formatDate post.publishedDate}}</time>
      {{{post.content}}}
    </article>
  {{/inline}}
{{/layout}}
